#import "../fragments/listingType.fragment.gql"

query GetListingTypes ($pagination: Pagination, $sort: Sortable) {
    listing_types (pagination: $pagination, sort: $sort) {
        data {
            ...ListingTypeFragment
        }
        total
        hasPages
        lastPage
    }
}
