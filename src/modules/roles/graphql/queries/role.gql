#import "../fragments/role.fragment.gql"

query RolesAll ($pagination: Pagination, $sort: Sortable) {
    roles (pagination: $pagination, sort: $sort) {
        data {
            ...RoleFragment
        }
        total
        hasPages
        lastPage
    }
}
