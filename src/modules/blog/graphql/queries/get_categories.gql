#import "../fragments/category.fragment.gql"

query GetCategories ($pagination: Pagination, $sort: Sortable) {
    categories (pagination: $pagination, sort: $sort) {
        data {
            ...CategoryFragment
        }
        total
        hasPages
        lastPage
    }
}
