mutation ForgotPassword ($email: String!, $route_update_password: String!) {
    forgotPassword (email: $email, route_update_password: $route_update_password) {
        status
        message
    }
}
